#pragma once
#include "MathLib4D.h"

class Plane
{
public:
    // Ax + By + Cz + D = 0
    // float width, height; 
    Vector3 point;
    Vector3 normal;
    // float x, y, z;
    // float nX, nY, nZ;

};